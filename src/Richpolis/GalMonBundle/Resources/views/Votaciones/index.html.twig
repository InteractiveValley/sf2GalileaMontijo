{% extends "RichpolisBackendBundle::backend.html.twig" %}

{% block menu_horizontal %}
    {% include 'RichpolisBackendBundle:Default:menu_horizontal.html.twig' with {'pagina': 'votaciones'} %}
{% endblock %}

{% block body %}
<h1>Votaciones</h1>

<table class="table table-hover records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Imagen</th>
            <th>Thumbnail</th>
            <th>Posicion</th>
            <th>Votacion</th>
            <th>Semana_id</th>
            <th>Activo</th>
            <th>Creado</th>
            <th>Actualizado</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('votaciones_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.imagen }}</td>
            <td>{{ entity.thumbnail }}</td>
            <td>{{ entity.posicion }}</td>
            <td>{{ entity.votacion }}</td>
            <td>{{ entity.semanaid }}</td>
            <td>{{ entity.isactive }}</td>
            <td>{% if entity.createdat %}{{ entity.createdat|date('d/m/Y H:i') }}{% endif%}</td>
            <td>{% if entity.updatedat %}{{ entity.updatedat|date('d/m/Y H:i') }}{% endif%}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('votaciones_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('votaciones_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul class="btn-group">
   <li>
       <a class="btn btn-primary" href="{{ path('votaciones_new') }}">
           Nuevo registro
       </a>
   </li>
</ul>
{% endblock %}